<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net8.0</TargetFramework>
		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>
		<IsPackable>false</IsPackable>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.1" />
		<PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
		<PackageReference Include="xunit" Version="2.9.2" />
		<PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
	</ItemGroup>

	<ItemGroup>
		<!-- Only exists after generate.sh has run - this project is built as part of the SDK's own
		     build-and-package flow (which always generates first), never as part of the main solution. -->
		<ProjectReference Include="..\..\generated\src\OrderEazi.Commerce.Sdk\OrderEazi.Commerce.Sdk.csproj" />
	</ItemGroup>

	<ItemGroup>
		<Compile Include="..\..\src\RetryHandler.cs" Link="RetryHandler.cs" />
	</ItemGroup>

</Project>
